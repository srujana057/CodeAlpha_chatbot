feat: add basic rule-based chatbot using Python

- Implemented chatbot using if-elif conditions
- Added continuous interaction with while loop
- Included predefined responses for common inputs (hello, how are you, name, bye)
- Handled user input using lower() and strip() for better matching
